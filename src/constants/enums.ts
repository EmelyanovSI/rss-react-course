export enum Severity {
  success = 'success',
  error = 'error',
  warning = 'warning',
  info = 'info',
}

export enum ViewMode {
  Compact = 'compact',
  Detailed = 'detailed',
}

export enum Status {
  Idle = 'idle',
  Pending = 'pending',
  Fulfilled = 'fulfilled',
  Rejected = 'rejected',
}
