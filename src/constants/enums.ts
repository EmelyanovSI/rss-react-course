export enum Severity {
  success = 'success',
  error = 'error',
  warning = 'warning',
  info = 'info',
}

export enum Status {
  Idle,
  Loading,
  Succeeded,
  Failed,
}

export enum ViewMode {
  Compact = 'compact',
  Detailed = 'detailed',
}
